[package]
name = "numworks-ereader"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
num_enum = { version = "0.7.6", default-features = false }
embedded-graphics = "0.8.2"